class Solution {
public:
    int maxDepth(string s) {
        int count =0;
        int maxCount=0;
        for(auto it: s){
            if(it == '(') count++;
            if(it == ')') count--;
            maxCount = max(count, maxCount);
        
        }
        return maxCount;
    }
};